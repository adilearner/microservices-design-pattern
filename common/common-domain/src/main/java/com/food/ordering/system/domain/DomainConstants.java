package com.food.ordering.system.domain;

public class DomainConstants {

    private DomainConstants() {
        /*
        Added private constructors to the class so that this class will only server
        for static fields, so I cannot create instance of this class because of this
        private constructors
         */
    }
    public static final String UTC = "UTC";
}
